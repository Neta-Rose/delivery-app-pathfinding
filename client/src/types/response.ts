export interface pathResponse {
    deliveryPath: [number, number][];
    deliveryVisited: [number, number][];
    deliveryMan: [number, number];
    userPath: [number, number][];
    userVisited: [number, number][];
}